//runs on object creation

//variable to check if level has been cleared by user, will be changed to true when user reaches end of level
if global.forest_clear = false {
	is_cleared = false;
}
else {
	is_cleared = true;
}